import { describe, expect, it } from "vitest"

// Placeholder integration: extend to fetch /diagnostics/providers when test infra is ready
describe("provider diagnostics", () => {
  it("placeholder", () => {
    expect(true).toBe(true)
  })
})
