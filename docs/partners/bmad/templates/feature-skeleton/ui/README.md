Feature **ui/** folder

- Render-only components: screens, widgets, and callouts.
- Import hooks/selectors from `application/` and types from `domain/`.
- Co-locate feature panels (e.g., `WorkspacePanels.tsx`) and lazy loaders.
