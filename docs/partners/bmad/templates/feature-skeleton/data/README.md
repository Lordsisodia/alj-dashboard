Optional **data/** folder

- Use only for demo fixtures or mock data needed during early development.
- Clearly label files as mock/demo and remove them once real services are wired.
- Never import data fixtures into `domain/` or `application/` without explicit gating.
