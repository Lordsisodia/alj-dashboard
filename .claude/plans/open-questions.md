# Open Questions

## creators-table-airtable-redesign - 2026-04-06
- [ ] Bulk action toolbar design — what actions appear when rows are selected (delete, export, tag, pause)? — Determines whether checkbox selection needs a toolbar component
- [ ] Sort-by-column interaction — should headers be clickable to sort now, or is that a separate pass? — Affects whether header cells need sort icons/affordances baked in
- [ ] Mobile / tablet breakpoint — does the table need a card-view fallback below 1024px, or is this desktop-only? — Sticky columns and 10-column grids don't work well on phones
- [ ] Foreplay vs Airtable tension — brain.md says "pixel-cloning Foreplay.co" but this brief asks for Airtable style. Which wins when they conflict? — Affects border thickness, hover color, header weight choices
