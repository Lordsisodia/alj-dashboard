# Training Spotlight — ui/screens

Primary route surfaces rendered by Next.js (e.g., `/partners/academy/training-spotlight`). Each screen should fetch data via hooks and own responsiveness + layout glue.
