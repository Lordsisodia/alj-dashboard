Feature **ui/** folder

- React components and screens only—no direct data fetching or business logic.
- Import hooks/selectors from `application/` and types from `domain/`.
- Co-locate screen-specific components under this folder.
