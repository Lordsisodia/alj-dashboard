# Recruitment — shared ui

Shared UI utilities and thin client components reused across recruitment pages.

- `RecruitmentNavSync.client.tsx` — keeps the drawer/navigation state consistent across recruitment routes.
- `RecruitmentWorkspace.tsx` — reusable hub shell for invites and referral workflows; wire into a page screen as needed.
