Feature **domain/** folder

- Define business entities, value objects, enums, constants (pure TypeScript).
- Add validation utilities or rule engines that do not depend on React or browser APIs.
- No imports from `application/`, `infrastructure/`, or `ui/`.
