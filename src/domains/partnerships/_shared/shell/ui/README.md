# Shell UI

- `Shell.tsx` — responsive shell (alias `MobileShell` kept for compatibility).
- `LazyShell.tsx` — dynamic/SSR-safe loader (alias `LazyMobileShell`).
- `components/` — view primitives (viewport, quick-action sheet).
- `quick-actions/` — renders quick-action targets/panels.
