# Shell application layer

- `navigation-store.tsx` — client nav state (tabs, drawer, quick actions, persistence).
- `tab-registry.ts` — tab metadata used by shell and quick actions.
- `quick-action-routes.ts` — maps quick actions to canonical routes.
- `shell-service.ts` — server-safe getter for tabs/quick actions.
