# Pipeline Operations (Portal Architecture)

> **Status:** migrated — see live code under `src/domains/partnerships/pipeline-ops`

The runtime implementation (domain, application, infrastructure, UI) now lives in `src/domains/partnerships/pipeline-ops`. This folder remains for historical documentation and planning references only.
