# Earnings (Shared Notes)

This folder now only stores cross-section references for the earnings suite.

- Primary implementation lives in the numbered folders `01-` → `09-`.
- Shared UI glue (navigation + back-links) sits in `shared/ui/components`.
- All section-specific docs were moved into their respective `docs/` folders.

Use this file for any global notes that apply to multiple earnings sections.
