# 01-general / infrastructure

- Gateways/services for General Settings (API clients, persistence, mocks).
- Currently empty; add service implementations here instead of UI hooks when wiring real data.
