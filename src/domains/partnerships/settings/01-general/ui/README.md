# 01-general / ui

- Presentation layer for General Settings (desktop + shared components).
- Hosts screens, components, and previews for quick settings navigation.
- Keep UI pure: pull data via hooks from `../application`.
