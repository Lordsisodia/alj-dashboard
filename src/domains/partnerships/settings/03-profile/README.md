# 03 Profile — Structure
- Purpose: partner profile (avatar, bio, public profile controls).
- Routes: `/partners/settings/profile`.
- Layout: application / data / docs / domain / infrastructure / ui (use same pattern as other sections).
- Shared utilities/components remain in `settings/shared/`.
