# 04 Devices — Structure
- Purpose: connected devices/sessions, revoke access.
- Routes: `/partners/settings/connected-devices`.
- Layout: `application/`, `data/`, `docs/`, `domain/`, `infrastructure/`, `ui/`.
- Pull shared shells/nav from `settings/shared/`.
