# Shared application (settings)

This folder is reserved for truly shared application-level utilities/hooks.
Page-specific application logic now lives in each settings page folder
(e.g., `01-general/application`, `02-my-account/application`, etc.).
