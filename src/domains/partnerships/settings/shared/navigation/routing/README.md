# settings/shared/navigation/routing

- Central registry + helpers for settings routes.
- Files:
  - `settings-route-registry.ts` — typed registry of settings routes and lazy loaders.
  - `route-renderers.tsx` — helper to resolve a slug and render the right view (SSR-safe).
