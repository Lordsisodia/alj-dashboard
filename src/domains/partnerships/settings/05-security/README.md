# 05 Security — Structure
- Purpose: 2FA, backup codes, session security.
- Routes: `/partners/settings/security`.
- Layout: application / data / docs / domain / infrastructure / ui.
- Shared components (page shell/menu) are in `settings/shared/`.
