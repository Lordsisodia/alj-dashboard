// Canonical source moved to @/components/ui/activity-feed
// This re-export keeps existing consumers (LogDashboard, etc.) working without changes.
export { ActivityFeed } from '@/components/ui/activity-feed';
