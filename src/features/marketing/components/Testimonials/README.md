# Testimonials

Testimonials carousel section with 6 customer quotes arranged in a 3-column grid. Each card shows quote, avatar, name, title, and bottom "Start" CTA button.

| File | Purpose |
|------|---------|
| `index.tsx` | Root export; renders section header, testimonial grid, CTA |

## Props & Data

No props. Static `TESTIMONIALS` array with:
- quote (text with smart quotes)
- name, title, avatar (image URL)

## Where it's used

Appears to be unused in main routing. Likely used in ProductPage or legacy sections.
