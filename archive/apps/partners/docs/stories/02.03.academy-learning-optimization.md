# Story 02.03: Academy & Learning Optimization

<!-- Powered by BMAD™ Core -->

## Status
Backlog

## Story
**As a** partner using the Academy to ramp quickly,
**I want** course/program pages to load within a second,
**so that** training flows stay responsive even with placeholder data.

## Acceptance Criteria

1. **Performance Budget**
   - `/partners/academy/courses/siso-essentials-program`, `/academy/my-progress`, `/academy/saved`, `/academy/training-spotlight` load ≤ 1,500 ms / FCP ≤ 1,200 ms (baseline up to 16,357 ms per TSV rows 4–15).
   - `/partners/learning` ≤ 1,000 ms load.

2. **Data Streaming & Pagination**
   - Course program modules + progress data streamed via RSC (`cache(fetch)` over JSON), no massive client arrays.
   - My Progress + Saved pages paginate or virtualize long lists (≤ 20 cards initial render).

3. **Client Hydration Minimization**
   - Lesson detail CTA widgets (copy/share, video players) lazy-loaded via `next/dynamic`.
   - `Waves` background replaced with CSS gradient on non-hero screens to reduce hydration cost.

4. **Instrumentation**
   - Updated baseline showing Academy routes under budget + analyzer screenshot proving parsed bundle ≤ 80 KB.
   - Checklist + plan updated; failure to meet budgets blocks completion.

## Tasks / Subtasks

- [ ] **Stream Course Programs** (AC: 1,2)
  - [ ] Move `courses` mock data into `public/data/academy/courses/*.json`.
  - [ ] Update `/academy/courses/[courseId]` to fetch via server helper + stream modules.
  - [ ] Add suspense fallback for module sections.

- [ ] **My Progress & Saved Pagination** (AC: 1,2)
  - [ ] Implement server-side slicing of progress cards.
  - [ ] Add pagination/virtual list component with skeleton placeholders.
  - [ ] Ensure filters/search operate without client re-fetch cost.

- [ ] **Hydration Trimming** (AC: 1,3)
  - [ ] Wrap video players, share buttons, and advanced CTAs in `next/dynamic`.
  - [ ] Replace `Waves` background with CSS gradient for My Progress + Saved routes.
  - [ ] Audit icon imports; tree-shake unused icons.

- [ ] **Measurement** (AC: 4)
  - [ ] Run `npm run perf:baseline` (new `BASELINE_DIR`).
  - [ ] Capture `.next/analyze` snippet for Academy routes ≤ 80 KB parsed.
  - [ ] Update plan + checklist; log before/after metrics in story.

## Dev Notes
- Academy already partially optimized but program route still hydrates entire `courses[]` list.
- Aligns with BMAD plan section “Academy & Learning.”

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2025-11-24 | 1.0 | Story drafted for Academy optimization | BMAD Agent |
